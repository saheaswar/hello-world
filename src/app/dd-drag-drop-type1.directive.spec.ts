import { DdDragDropType1Directive } from './dd-drag-drop-type1.directive';

describe('DdDragDropType1Directive', () => {
  it('should create an instance', () => {
    const directive = new DdDragDropType1Directive();
    expect(directive).toBeTruthy();
  });
});
